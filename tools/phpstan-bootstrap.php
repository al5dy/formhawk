<?php

if ( ! defined( 'FORMHAWK_VERSION' ) ) {
	define( 'FORMHAWK_VERSION', '0.5.0' );
}

if ( ! defined( 'FORMHAWK_DB_VERSION' ) ) {
	define( 'FORMHAWK_DB_VERSION', '6' );
}

if ( ! defined( 'FORMHAWK_FILE' ) ) {
	define( 'FORMHAWK_FILE', __FILE__ );
}

if ( ! defined( 'FORMHAWK_DIR' ) ) {
	define( 'FORMHAWK_DIR', __DIR__ . '/../' );
}

if ( ! defined( 'FORMHAWK_URL' ) ) {
	define( 'FORMHAWK_URL', 'https://example.test/wp-content/plugins/formhawk/' );
}
